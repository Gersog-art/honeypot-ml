⭐ Если проект полезен, поставьте звезду на GitHub!
text


## **📚 2. ДОКУМЕНТАЦИЯ В ПАПКЕ docs/**

### **2.1 docs/INSTALLATION.md**

```markdown
# 📦 Установка HONEYPOT-ML системы

## Требования к системе

### Минимальные требования
- **ОС**: Ubuntu 20.04+, Debian 11+, Kali Linux, macOS 10.15+, WSL2
- **Память**: 2 GB RAM
- **Диск**: 5 GB свободного места
- **Docker**: версия 20.10+
- **Python**: версия 3.8+

### Рекомендуемые требования
- **ОС**: Ubuntu 22.04 LTS
- **Память**: 4 GB RAM
- **Диск**: 10 GB свободного места
- **Процессор**: 2+ ядра

## Установка на Linux (Ubuntu/Debian/Kali)

### Шаг 1: Установка Docker

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
sudo apt install docker.io docker-compose -y

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER

# Проверка установки
docker --version

Шаг 2: Установка Python и зависимостей
bash

# Установка Python3 и pip
sudo apt install python3 python3-pip python3-venv -y

# Создание виртуального окружения (рекомендуется)
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# или на Windows: venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

Шаг 3: Клонирование проекта
bash

# Клонирование репозитория
git clone https://github.com/yourusername/honeypot-ml-system.git
cd honeypot-ml-system

# Настройка прав
chmod +x run_simple.py scripts/**/*.py

Установка на macOS
Шаг 1: Установка Homebrew
bash

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

Шаг 2: Установка Docker и Python
bash

# Установка Docker Desktop
brew install --cask docker

# Установка Python
brew install python3

# Запуск Docker Desktop (через Applications)

Шаг 3: Установка проекта
bash

# Клонирование
git clone https://github.com/yourusername/honeypot-ml-system.git
cd honeypot-ml-system

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

Установка на Windows (через WSL2)
Шаг 1: Установка WSL2

    Откройте PowerShell от имени администратора

    Выполните:

powershell

wsl --install

    Перезагрузите компьютер

    Установите Ubuntu из Microsoft Store

Шаг 2: Настройка WSL2
bash

# В Ubuntu WSL2
sudo apt update && sudo apt upgrade -y

# Установка Docker
sudo apt install docker.io -y

Шаг 3: Установка проекта

Следуйте инструкциям для Linux выше.
Быстрая проверка установки
bash

# Запустите проверочный скрипт
python3 -c "
import sys
print('✅ Python версия:', sys.version)
import docker
print('✅ Docker доступен')
import sklearn
print('✅ Scikit-learn установлен')
print('\n✅ Все зависимости установлены правильно!')
"

Решение проблем
Проблема: Docker требует sudo
bash

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Выйдите и войдите заново
# или выполните:
newgrp docker

Проблема: Порты 3000/3001 заняты
bash

# Освобождение портов
sudo fuser -k 3000/tcp
sudo fuser -k 3001/tcp

# Или используйте другие порты
python3 run_simple.py --port 8080

Проблема: Ошибки Python зависимостей
bash

# Обновите pip
pip install --upgrade pip

# Переустановите зависимости
pip uninstall -r requirements.txt -y
pip install -r requirements.txt

Проблема: Honeypot не запускается
bash

# Проверьте Docker
docker ps
docker logs <container_id>

# Очистите старые контейнеры
docker stop $(docker ps -q)
docker rm $(docker ps -a -q)

Проверка установки
bash

# Полный тест системы
./test_installation.sh

# Или пошагово
docker --version          # Должна быть версия 20+
python3 --version         # Должна быть 3.8+
pip list | grep scikit    # Должен быть scikit-learn

Готово! 🎉

Система установлена. Запустите:
bash

python3 run_simple.py

Или перейдите к документации по использованию.
text


### **2.2 docs/USAGE.md**

```markdown
# 🎮 Использование HONEYPOT-ML системы

## Быстрый старт

### Вариант 1: Полная автоматизация (рекомендуется)

```bash
# Один скрипт запускает всю систему
python3 run_simple.py

Что произойдет:

    ✅ Остановка старых процессов

    ✅ Запуск OWASP Juice Shop на порту 3000

    ✅ Создание/загрузка ML модели

    ✅ Запуск прокси-монитора на порту 3001

    ✅ Автоматические тестовые атаки

    ✅ Отображение обнаружений в реальном времени

Вариант 2: Ручное управление
Терминал 1: Honeypot
bash

# Запуск honeypot
docker run -d -p 3000:3000 --name juice-shop bkimminich/juice-shop

# Проверка
curl http://localhost:3000

Терминал 2: Монитор
bash

# Запуск монитора (требует sudo для портов < 1024)
sudo python3 scripts/core/simple_proxy.py --listen-port 3001

# Или без sudo на высоких портах
python3 scripts/core/simple_proxy.py --listen-port 8080 --target-port 3000

Терминал 3: Тестирование
bash

# Нормальные запросы
curl "http://localhost:3001/"
curl "http://localhost:3001/#/login"

# SQL Injection
curl "http://localhost:3001/rest/products/search?q=test'OR'1'='1"

# XSS (используйте кодирование)
curl -G "http://localhost:3001/rest/products/search" --data-urlencode "q=<script>alert('XSS')</script>"

# Path Traversal
curl "http://localhost:3001/assets/../../../etc/passwd"

# Command Injection
curl "http://localhost:3001/rest/products/search?q='; ls -la /"

Режимы работы
Режим 1: Реальное обнаружение (рекомендуется)
bash

python3 run_simple.py
# Или
python3 scripts/core/simple_proxy.py

Особенности:

    Захват реального сетевого трафика

    Обнаружение в реальном времени

    Статистика и логирование

Режим 2: Демо-симуляция
bash

python3 scripts/core/web_monitor.py

Выберите режимы:

    1 - Тестовая симуляция - автоматическая отправка тестовых атак

    2 - HTTP сервер - создание тестового сервера на порту 8080

    3 - Ручной ввод - анализ URL вручную

Режим 3: ML тестирование
bash

# Обучение новой модели
python3 scripts/ml/train_model.py

# Тестирование модели
python3 -c "
import joblib
model = joblib.load('ml_models/attack_detector_model.pkl')
print('Модель загружена:', model)
"
если есть проблема honeypot не запущен то надо освободить порты для докера sudo fuser -k 3000/tcp
sudo fuser -k 3001/tcp
docker stop $(docker ps -q) 2>/dev/null
docker rm $(docker ps -a -q) 2>/dev/null
